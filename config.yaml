namespaces:
  trino-dev:
    deployments:
      trino-dev-worker:
        minReplicas: 1
        maxReplicas: 3
        scaleUpBatchSize: 2
        scaleDownBatchSize: 1
      trino-dev-coordinator:
        minReplicas: 1
        maxReplicas: 1
        scaleUpBatchSize: 0
        scaleDownBatchSize: 0
  trino-adhoc:
    deployments:
      trino-adhoc-worker:
        minReplicas: 5
        maxReplicas: 15
        scaleUpBatchSize: 3
        scaleDownBatchSize: 4
